import { PositiveValueDirective } from './positive-value.directive';

describe('PositiveValueDirective', () => {
  it('should create an instance', () => {
    const directive = new PositiveValueDirective();
    expect(directive).toBeTruthy();
  });
});
